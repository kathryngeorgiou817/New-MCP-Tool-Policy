# CDSSearch

Import from `@ciscodesignsystems/cds-react-search`.

For full props and usage, use the Magnetic MCP tool `mcp__magnetic-dev__ask_react_components` to query this component's documentation.

CDSSearch uses dot notation:

- `CDSSearch.Simple` — simple search input with `placeholder` and `size` props
